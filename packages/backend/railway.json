{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS_V1",
    "buildCommand": "cd packages/backend && npm install && npx prisma generate --schema=prisma/schema.postgresql.prisma && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd packages/backend && node dist/server.js"
  }
}
