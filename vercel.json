{
  "buildCommand": "npm run build -w packages/backend && npm run build -w packages/frontend",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "npm install",
  "routes": [
    { "src": "/api/(.*)", "dest": "/packages/backend/dist/server.js" }
  ]
}
